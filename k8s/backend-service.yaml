apiVersion: v1
kind: Service
metadata:
  name: jition-backend-service
  namespace: default
spec:
  selector:
    app: jition-backend
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP
